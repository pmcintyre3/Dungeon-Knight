/**
 * Created by Phillip on 2/28/2015.
 */
ig.module(
    'game.entities.rope'
)
.requires(
    'impact.entity'
    //'plugins.box2d.game',
    //'plugins.box2d.entity'
)
.defines(function(){
       EntityRope = ig.Entity.extend({

       });
    });